build = "npm run build"
artifact = "dist/main.js"
static = "out"

env = [
  "githubClientId",
  "allowedGithubUsers",
  { key = "icpBeian", optional = true },
]

secrets = [
  "githubClientSecret",
  "jwtSecretKey",
]

mysql = [
  "primary",
]
